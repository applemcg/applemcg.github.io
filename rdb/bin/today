: today -- gregorian -c

: ${YYMMDD:=`date '+%y%m%d'`}
export YYMMDD
if [ $# -lt 1 ]
then
    echo $YYMMDD
else
    expr $(julian $YYMMDD) \
    $(
       echo $1 |
       sed 's/\([+-]\)\(.*\)/\1 \2/'
    ) |
    gregorian
fi


